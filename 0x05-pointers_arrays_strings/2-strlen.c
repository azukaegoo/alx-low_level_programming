#include "main.h"
#include <string.h>
/**
 * _strlen - calculate the length of the string pointed to
 * int _strlen(char *s) - function defintion to return the length of a string
 * @s: parameter name
 * Return: s
*/
int _strlen(char *s)
{

	return (strlen(s));
}
