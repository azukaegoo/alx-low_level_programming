#include "main.h"
/**
 * mul - multiplies two integers
 * int mul(int a, int b) - multiplying two integers
 * @a: parameter name for integer one
 * @b: parameter name for integer two
 * Return: total
*/
int mul(int a, int b)
{

	int total;

	total = (a * b);
	{
	return (total);
	}
}
