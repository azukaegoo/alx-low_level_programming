#include "main.h"
/**
 * add - adding two integers
 * int add(int, int) - function definition that adds two integers
 * @numOne: the integer
 * @numTwo: the integer
 * Return: total
*/
int add(int numOne, int numTwo)
{

	int total;

	total = numOne + numTwo;
	return (total);
}
